the_whole_shebang():
- w a i t
- closing():
  - wait ssto b worm e e d d c c ssto b worm ssto b worm worm ssto b a ssto
  - more_pistons(0):
    - d
  - extend(1):
    - b
    - a
  - s t o p
- s t o p
- opening():
  - a
  - retract(1):
    - pull(-1):
      - b
    - row_high(0, True):
      - full_extend(0, True):
        - extend(0, True, needs_parity=True):
          - a
        - extra_pulses(0, True):
      - retract(0):
        - pull(-2):
        - shift_pistons(0):
          - d c c
        - row_high(-1, True):
          - full_extend(-1, True):
            - extend(-1, True, needs_parity=True):
              - b
            - extra_pulses(-1, True):
          - retract(-1):
  - s s t o
  - full_row(2):
    - more_pistons(1):
      - e
    - row_high(2):
      - full_extend(2, False):
        - extend(2, False, needs_parity=True):
          - b
          - more_obs(True):
            - bobs bobs
          - extend(-1, needs_parity=True):
            - b
        - extra_pulses(2, False):
          - b
      - retract(2):
        - less_obs():
          - ssto ssto
        - pull(0):
          - more_pistons(0):
            - d
          - full_extend(0):
            - extend(0, False, needs_parity=True):
              - a
            - extra_pulses(0, False):
          - retract(-3):
          - full_row(-2):
          - shift_pistons(0):
            - d c c
        - shift_pistons(1):
          - e d
        - row_high(1, True):
          - full_extend(1, True):
            - extend(1, True, needs_parity=True):
              - a
            - extra_pulses(1, True):
          - retract(1):
            - pull(-1):
              - b
            - row_high(0, True):
              - full_extend(0, True):
                - extend(0, True, needs_parity=True):
                  - a
                - extra_pulses(0, True):
              - retract(0):
                - pull(-2):
                - shift_pistons(0):
                  - d c c
                - row_high(-1, True):
                  - full_extend(-1, True):
                    - extend(-1, True, needs_parity=True):
                      - b
                    - extra_pulses(-1, True):
                  - retract(-1):
  - storage_moves(a, b, ssto):
  - full_row(3):
    - more_pistons(1):
      - e
    - row_high(3):
      - full_extend(3, False):
        - extend(3, False, needs_parity=True):
          - b
          - more_obs(True):
            - bobs bobs
          - more_pistons(0):
            - d
          - extend(0, needs_parity=True):
            - a
        - extra_pulses(3, False):
          - a
      - retract(3):
        - retract(0):
          - pull(-2):
          - shift_pistons(0):
            - d c c
          - row_high(-1, True):
            - full_extend(-1, True):
              - extend(-1, True, needs_parity=True):
                - b
              - extra_pulses(-1, True):
            - retract(-1):
        - less_obs():
          - ssto ssto
        - pull(1):
          - more_pistons(0):
            - d
          - full_extend(1):
            - extend(1, False, needs_parity=True):
              - b
              - a
            - extra_pulses(1, False):
          - retract(-2):
          - full_row(-1):
            - row_high(-1):
              - full_extend(-1, False):
                - extend(-1, False, needs_parity=True):
                  - b
                - extra_pulses(-1, False):
              - retract(-1):
          - shift_pistons(0):
            - d c c
        - row_high(2, True):
          - full_extend(2, True):
            - extend(2, True, needs_parity=True):
              - more_obs(True):
                - bobs bobs
              - extend(-1, needs_parity=True):
                - b
            - extra_pulses(2, True):
              - b
          - retract(2):
            - less_obs():
              - ssto ssto
            - pull(0):
              - more_pistons(0):
                - d
              - full_extend(0):
                - extend(0, False, needs_parity=True):
                  - a
                - extra_pulses(0, False):
              - retract(-3):
              - full_row(-2):
              - shift_pistons(0):
                - d c c
            - shift_pistons(1):
              - e d
            - row_high(1, True):
              - full_extend(1, True):
                - extend(1, True, needs_parity=True):
                  - a
                - extra_pulses(1, True):
              - retract(1):
                - pull(-1):
                  - b
                - row_high(0, True):
                  - full_extend(0, True):
                    - extend(0, True, needs_parity=True):
                      - a
                    - extra_pulses(0, True):
                  - retract(0):
                    - pull(-2):
                    - shift_pistons(0):
                      - d c c
                    - row_high(-1, True):
                      - full_extend(-1, True):
                        - extend(-1, True, needs_parity=True):
                          - b
                        - extra_pulses(-1, True):
                      - retract(-1):
  - storage_moves(a, ssto):
  - full_row(4):
    - more_pistons(2):
      - e fold
    - row_high(4):
      - full_extend(4, False):
        - extend(4, False, needs_parity=True):
          - b
          - more_obs(True):
            - bobs bobs
          - more_pistons(0):
            - d
          - extend(1, needs_parity=True):
            - b
            - a
        - extra_pulses(4, False):
          - a
      - retract(4):
        - retract(1):
          - pull(-1):
            - b
          - row_high(0, True):
            - full_extend(0, True):
              - extend(0, True, needs_parity=True):
                - a
              - extra_pulses(0, True):
            - retract(0):
              - pull(-2):
              - shift_pistons(0):
                - d c c
              - row_high(-1, True):
                - full_extend(-1, True):
                  - extend(-1, True, needs_parity=True):
                    - b
                  - extra_pulses(-1, True):
                - retract(-1):
        - less_obs():
          - ssto ssto
        - pull(2):
          - more_pistons(1):
            - e
          - full_extend(2):
            - extend(2, False, needs_parity=True):
              - b
              - more_obs(True):
                - bobs bobs
              - extend(-1, needs_parity=True):
                - b
            - extra_pulses(2, False):
              - b
          - retract(-1):
          - less_obs():
            - ssto ssto
          - full_row(0):
            - more_pistons(0):
              - d
            - row_high(0):
              - full_extend(0, False):
                - extend(0, False, needs_parity=True):
                  - a
                - extra_pulses(0, False):
              - retract(0):
                - pull(-2):
                - shift_pistons(0):
                  - d c c
                - row_high(-1, True):
                  - full_extend(-1, True):
                    - extend(-1, True, needs_parity=True):
                      - b
                    - extra_pulses(-1, True):
                  - retract(-1):
          - shift_pistons(1):
            - e d
          - shift_pistons(0):
            - d c c
        - shift_pistons(2):
          - f o l d
        - row_high(3, True):
          - full_extend(3, True):
            - extend(3, True, needs_parity=True):
              - more_obs(True):
                - bobs bobs
              - more_pistons(0):
                - d
              - extend(0, needs_parity=True):
                - a
            - extra_pulses(3, True):
              - a
          - retract(3):
            - retract(0):
              - pull(-2):
              - shift_pistons(0):
                - d c c
              - row_high(-1, True):
                - full_extend(-1, True):
                  - extend(-1, True, needs_parity=True):
                    - b
                  - extra_pulses(-1, True):
                - retract(-1):
            - less_obs():
              - ssto ssto
            - pull(1):
              - more_pistons(0):
                - d
              - full_extend(1):
                - extend(1, False, needs_parity=True):
                  - b
                  - a
                - extra_pulses(1, False):
              - retract(-2):
              - full_row(-1):
                - row_high(-1):
                  - full_extend(-1, False):
                    - extend(-1, False, needs_parity=True):
                      - b
                    - extra_pulses(-1, False):
                  - retract(-1):
              - shift_pistons(0):
                - d c c
            - row_high(2, True):
              - full_extend(2, True):
                - extend(2, True, needs_parity=True):
                  - more_obs(True):
                    - bobs bobs
                  - extend(-1, needs_parity=True):
                    - b
                - extra_pulses(2, True):
                  - b
              - retract(2):
                - less_obs():
                  - ssto ssto
                - pull(0):
                  - more_pistons(0):
                    - d
                  - full_extend(0):
                    - extend(0, False, needs_parity=True):
                      - a
                    - extra_pulses(0, False):
                  - retract(-3):
                  - full_row(-2):
                  - shift_pistons(0):
                    - d c c
                - shift_pistons(1):
                  - e d
                - row_high(1, True):
                  - full_extend(1, True):
                    - extend(1, True, needs_parity=True):
                      - a
                    - extra_pulses(1, True):
                  - retract(1):
                    - pull(-1):
                      - b
                    - row_high(0, True):
                      - full_extend(0, True):
                        - extend(0, True, needs_parity=True):
                          - a
                        - extra_pulses(0, True):
                      - retract(0):
                        - pull(-2):
                        - shift_pistons(0):
                          - d c c
                        - row_high(-1, True):
                          - full_extend(-1, True):
                            - extend(-1, True, needs_parity=True):
                              - b
                            - extra_pulses(-1, True):
                          - retract(-1):
  - storage_moves(a, ssto):
  - full_row(5):
    - more_pistons(2):
      - e fold
    - row_high(5):
      - full_extend(5, False):
        - extend(5, False, needs_parity=True):
          - b
          - more_obs(True):
            - bobs bobs
          - more_pistons(1):
            - e
          - extend(2, needs_parity=True):
            - b
            - more_obs(True):
              - bobs ssto bobs bobs a bobs bobs bobs
            - extend(-1, needs_parity=True):
              - b
        - extra_pulses(5, False):
          - b b b b b
      - retract(5):
        - retract(2):
          - less_obs():
            - ssto ssto
          - pull(0):
            - more_pistons(0):
              - d
            - full_extend(0):
              - extend(0, False, needs_parity=True):
                - a
              - extra_pulses(0, False):
            - retract(-3):
            - full_row(-2):
            - shift_pistons(0):
              - d c c
          - shift_pistons(1):
            - e d
          - row_high(1, True):
            - full_extend(1, True):
              - extend(1, True, needs_parity=True):
                - a
              - extra_pulses(1, True):
            - retract(1):
              - pull(-1):
                - b
              - row_high(0, True):
                - full_extend(0, True):
                  - extend(0, True, needs_parity=True):
                    - a
                  - extra_pulses(0, True):
                - retract(0):
                  - pull(-2):
                  - shift_pistons(0):
                    - d c c
                  - row_high(-1, True):
                    - full_extend(-1, True):
                      - extend(-1, True, needs_parity=True):
                        - b
                      - extra_pulses(-1, True):
                    - retract(-1):
        - less_obs():
          - ssto ssto
        - pull(3):
          - more_pistons(1):
            - e
          - full_extend(3):
            - extend(3, False, needs_parity=True):
              - b
              - more_obs(True):
                - bobs bobs
              - more_pistons(0):
                - d
              - extend(0, needs_parity=True):
                - a
            - extra_pulses(3, False):
              - a
          - retract(0):
            - pull(-2):
            - shift_pistons(0):
              - d c c
            - row_high(-1, True):
              - full_extend(-1, True):
                - extend(-1, True, needs_parity=True):
                  - b
                - extra_pulses(-1, True):
              - retract(-1):
          - less_obs():
            - ssto ssto
          - full_row(1):
            - more_pistons(0):
              - d
            - row_high(1):
              - full_extend(1, False):
                - extend(1, False, needs_parity=True):
                  - b
                  - a
                - extra_pulses(1, False):
              - retract(1):
                - pull(-1):
                  - b
                - row_high(0, True):
                  - full_extend(0, True):
                    - extend(0, True, needs_parity=True):
                      - a
                    - extra_pulses(0, True):
                  - retract(0):
                    - pull(-2):
                    - shift_pistons(0):
                      - d c c
                    - row_high(-1, True):
                      - full_extend(-1, True):
                        - extend(-1, True, needs_parity=True):
                          - b
                        - extra_pulses(-1, True):
                      - retract(-1):
          - shift_pistons(1):
            - e d
          - shift_pistons(0):
            - d c c
        - row_high(4, True):
          - full_extend(4, True):
            - extend(4, True, needs_parity=True):
              - more_obs(True):
                - bobs bobs
              - more_pistons(0):
                - d
              - extend(1, needs_parity=True):
                - b
                - a
            - extra_pulses(4, True):
              - a
          - retract(4):
            - retract(1):
              - pull(-1):
                - b
              - row_high(0, True):
                - full_extend(0, True):
                  - extend(0, True, needs_parity=True):
                    - a
                  - extra_pulses(0, True):
                - retract(0):
                  - pull(-2):
                  - shift_pistons(0):
                    - d c c
                  - row_high(-1, True):
                    - full_extend(-1, True):
                      - extend(-1, True, needs_parity=True):
                        - b
                      - extra_pulses(-1, True):
                    - retract(-1):
            - less_obs():
              - ssto ssto
            - pull(2):
              - more_pistons(1):
                - e
              - full_extend(2):
                - extend(2, False, needs_parity=True):
                  - b
                  - more_obs(True):
                    - bobs bobs
                  - extend(-1, needs_parity=True):
                    - b
                - extra_pulses(2, False):
                  - b
              - retract(-1):
              - less_obs():
                - ssto ssto
              - full_row(0):
                - more_pistons(0):
                  - d
                - row_high(0):
                  - full_extend(0, False):
                    - extend(0, False, needs_parity=True):
                      - a
                    - extra_pulses(0, False):
                  - retract(0):
                    - pull(-2):
                    - shift_pistons(0):
                      - d c c
                    - row_high(-1, True):
                      - full_extend(-1, True):
                        - extend(-1, True, needs_parity=True):
                          - b
                        - extra_pulses(-1, True):
                      - retract(-1):
              - shift_pistons(1):
                - e d
              - shift_pistons(0):
                - d c c
            - shift_pistons(2):
              - f o l d
            - row_high(3, True):
              - full_extend(3, True):
                - extend(3, True, needs_parity=True):
                  - more_obs(True):
                    - bobs bobs
                  - more_pistons(0):
                    - d
                  - extend(0, needs_parity=True):
                    - a
                - extra_pulses(3, True):
                  - a
              - retract(3):
                - retract(0):
                  - pull(-2):
                  - shift_pistons(0):
                    - d c c
                  - row_high(-1, True):
                    - full_extend(-1, True):
                      - extend(-1, True, needs_parity=True):
                        - b
                      - extra_pulses(-1, True):
                    - retract(-1):
                - less_obs():
                  - ssto ssto
                - pull(1):
                  - more_pistons(0):
                    - d
                  - full_extend(1):
                    - extend(1, False, needs_parity=True):
                      - b
                      - a
                    - extra_pulses(1, False):
                  - retract(-2):
                  - full_row(-1):
                    - row_high(-1):
                      - full_extend(-1, False):
                        - extend(-1, False, needs_parity=True):
                          - b
                        - extra_pulses(-1, False):
                      - retract(-1):
                  - shift_pistons(0):
                    - d c c
                - row_high(2, True):
                  - full_extend(2, True):
                    - extend(2, True, needs_parity=True):
                      - more_obs(True):
                        - bobs bobs
                      - extend(-1, needs_parity=True):
                        - b
                    - extra_pulses(2, True):
                      - b
                  - retract(2):
                    - less_obs():
                      - ssto ssto
                    - pull(0):
                      - more_pistons(0):
                        - d
                      - full_extend(0):
                        - extend(0, False, needs_parity=True):
                          - a
                        - extra_pulses(0, False):
                      - retract(-3):
                      - full_row(-2):
                      - shift_pistons(0):
                        - d c c
                    - shift_pistons(1):
                      - e d
                    - row_high(1, True):
                      - full_extend(1, True):
                        - extend(1, True, needs_parity=True):
                          - a
                        - extra_pulses(1, True):
                      - retract(1):
                        - pull(-1):
                          - b
                        - row_high(0, True):
                          - full_extend(0, True):
                            - extend(0, True, needs_parity=True):
                              - a
                            - extra_pulses(0, True):
                          - retract(0):
                            - pull(-2):
                            - shift_pistons(0):
                              - d c c
                            - row_high(-1, True):
                              - full_extend(-1, True):
                                - extend(-1, True, needs_parity=True):
                                  - b
                                - extra_pulses(-1, True):
                              - retract(-1):
  - storage_moves(a, ssto):
  - full_row(6):
    - more_pistons(3):
      - e fold
    - row_high(6):
      - full_extend(6, False):
        - extend(6, False, needs_parity=True):
          - b
          - more_obs(True):
            - bobs bobs
          - more_pistons(1):
            - e
          - extend(3, needs_parity=True):
            - b
            - more_obs(True):
              - bobs ssto bobs bobs a bobs bobs bobs
            - more_pistons(0):
              - d
            - extend(0, needs_parity=True):
              - a
        - extra_pulses(6, False):
          - a a a a a a a
      - retract(6):
        - retract(3):
          - retract(0):
            - pull(-2):
            - shift_pistons(0):
              - d c c
            - row_high(-1, True):
              - full_extend(-1, True):
                - extend(-1, True, needs_parity=True):
                  - b
                - extra_pulses(-1, True):
              - retract(-1):
          - less_obs():
            - ssto ssto
          - pull(1):
            - more_pistons(0):
              - d
            - full_extend(1):
              - extend(1, False, needs_parity=True):
                - b
                - a
              - extra_pulses(1, False):
            - retract(-2):
            - full_row(-1):
              - row_high(-1):
                - full_extend(-1, False):
                  - extend(-1, False, needs_parity=True):
                    - b
                  - extra_pulses(-1, False):
                - retract(-1):
            - shift_pistons(0):
              - d c c
          - row_high(2, True):
            - full_extend(2, True):
              - extend(2, True, needs_parity=True):
                - more_obs(True):
                  - bobs ssto bobs bobs a bobs bobs bobs
                - extend(-1, needs_parity=True):
                  - b
              - extra_pulses(2, True):
                - b
            - retract(2):
              - less_obs():
                - ssto ssto
              - pull(0):
                - more_pistons(0):
                  - d
                - full_extend(0):
                  - extend(0, False, needs_parity=True):
                    - a
                  - extra_pulses(0, False):
                - retract(-3):
                - full_row(-2):
                - shift_pistons(0):
                  - d c c
              - shift_pistons(1):
                - e d
              - row_high(1, True):
                - full_extend(1, True):
                  - extend(1, True, needs_parity=True):
                    - a
                  - extra_pulses(1, True):
                - retract(1):
                  - pull(-1):
                    - b
                  - row_high(0, True):
                    - full_extend(0, True):
                      - extend(0, True, needs_parity=True):
                        - a
                      - extra_pulses(0, True):
                    - retract(0):
                      - pull(-2):
                      - shift_pistons(0):
                        - d c c
                      - row_high(-1, True):
                        - full_extend(-1, True):
                          - extend(-1, True, needs_parity=True):
                            - b
                          - extra_pulses(-1, True):
                        - retract(-1):
        - less_obs():
          - ssto ssto
        - pull(4):
          - more_pistons(2):
            - e
          - full_extend(4):
            - extend(4, False, needs_parity=True):
              - b
              - more_obs(True):
                - bobs bobs
              - more_pistons(0):
                - d
              - extend(1, needs_parity=True):
                - b
                - a
            - extra_pulses(4, False):
              - a
          - retract(1):
            - pull(-1):
              - b
            - row_high(0, True):
              - full_extend(0, True):
                - extend(0, True, needs_parity=True):
                  - a
                - extra_pulses(0, True):
              - retract(0):
                - pull(-2):
                - shift_pistons(0):
                  - d c c
                - row_high(-1, True):
                  - full_extend(-1, True):
                    - extend(-1, True, needs_parity=True):
                      - b
                    - extra_pulses(-1, True):
                  - retract(-1):
          - less_obs():
            - ssto ssto
          - full_row(2):
            - more_pistons(1):
              - d
            - row_high(2):
              - full_extend(2, False):
                - extend(2, False, needs_parity=True):
                  - b
                  - more_obs(True):
                    - bobs bobs
                  - extend(-1, needs_parity=True):
                    - b
                - extra_pulses(2, False):
                  - b
              - retract(2):
                - less_obs():
                  - ssto ssto
                - pull(0):
                  - more_pistons(0):
                    - c
                  - full_extend(0):
                    - extend(0, False, needs_parity=True):
                      - a
                    - extra_pulses(0, False):
                  - retract(-3):
                  - full_row(-2):
                  - shift_pistons(0):
                    - c
                - shift_pistons(1):
                - row_high(1, True):
                  - full_extend(1, True):
                    - extend(1, True, needs_parity=True):
                      - a
                    - extra_pulses(1, True):
                  - retract(1):
                    - pull(-1):
                      - b
                    - row_high(0, True):
                      - full_extend(0, True):
                        - extend(0, True, needs_parity=True):
                          - a
                        - extra_pulses(0, True):
                      - retract(0):
                        - pull(-2):
                        - shift_pistons(0):
                          - d c c
                        - row_high(-1, True):
                          - full_extend(-1, True):
                            - extend(-1, True, needs_parity=True):
                              - b
                            - extra_pulses(-1, True):
                          - retract(-1):
          - shift_pistons(2):
          - shift_pistons(1):
            - e d
          - shift_pistons(0):
            - d c c
        - shift_pistons(3):
        - row_high(5, True):
          - full_extend(5, True):
            - extend(5, True, needs_parity=True):
              - more_obs(True):
                - bobs bobs
              - more_pistons(1):
                - e
              - extend(2, needs_parity=True):
                - b
                - more_obs(True):
                  - bobs ssto bobs bobs a bobs bobs bobs
                - extend(-1, needs_parity=True):
                  - b
            - extra_pulses(5, True):
              - b b b
          - retract(5):
            - retract(2):
              - less_obs():
                - ssto ssto
              - pull(0):
                - more_pistons(0):
                  - d
                - full_extend(0):
                  - extend(0, False, needs_parity=True):
                    - a
                  - extra_pulses(0, False):
                - retract(-3):
                - full_row(-2):
                - shift_pistons(0):
                  - d c c
              - shift_pistons(1):
                - e d
              - row_high(1, True):
                - full_extend(1, True):
                  - extend(1, True, needs_parity=True):
                    - a
                  - extra_pulses(1, True):
                - retract(1):
                  - pull(-1):
                    - b
                  - row_high(0, True):
                    - full_extend(0, True):
                      - extend(0, True, needs_parity=True):
                        - a
                      - extra_pulses(0, True):
                    - retract(0):
                      - pull(-2):
                      - shift_pistons(0):
                        - d c c
                      - row_high(-1, True):
                        - full_extend(-1, True):
                          - extend(-1, True, needs_parity=True):
                            - b
                          - extra_pulses(-1, True):
                        - retract(-1):
            - less_obs():
              - ssto ssto
            - pull(3):
              - more_pistons(1):
                - e
              - full_extend(3):
                - extend(3, False, needs_parity=True):
                  - b
                  - more_obs(True):
                    - bobs bobs
                  - more_pistons(0):
                    - d
                  - extend(0, needs_parity=True):
                    - a
                - extra_pulses(3, False):
                  - a
              - retract(0):
                - pull(-2):
                - shift_pistons(0):
                  - d c c
                - row_high(-1, True):
                  - full_extend(-1, True):
                    - extend(-1, True, needs_parity=True):
                      - b
                    - extra_pulses(-1, True):
                  - retract(-1):
              - less_obs():
                - ssto ssto
              - full_row(1):
                - more_pistons(0):
                  - d
                - row_high(1):
                  - full_extend(1, False):
                    - extend(1, False, needs_parity=True):
                      - b
                      - a
                    - extra_pulses(1, False):
                  - retract(1):
                    - pull(-1):
                      - b
                    - row_high(0, True):
                      - full_extend(0, True):
                        - extend(0, True, needs_parity=True):
                          - a
                        - extra_pulses(0, True):
                      - retract(0):
                        - pull(-2):
                        - shift_pistons(0):
                          - d c c
                        - row_high(-1, True):
                          - full_extend(-1, True):
                            - extend(-1, True, needs_parity=True):
                              - b
                            - extra_pulses(-1, True):
                          - retract(-1):
              - shift_pistons(1):
                - e d
              - shift_pistons(0):
                - d c c
            - row_high(4, True):
              - full_extend(4, True):
                - extend(4, True, needs_parity=True):
                  - more_obs(True):
                    - bobs bobs
                  - more_pistons(0):
                    - d
                  - extend(1, needs_parity=True):
                    - b
                    - a
                - extra_pulses(4, True):
                  - a
              - retract(4):
                - retract(1):
                  - pull(-1):
                    - b
                  - row_high(0, True):
                    - full_extend(0, True):
                      - extend(0, True, needs_parity=True):
                        - a
                      - extra_pulses(0, True):
                    - retract(0):
                      - pull(-2):
                      - shift_pistons(0):
                        - d c c
                      - row_high(-1, True):
                        - full_extend(-1, True):
                          - extend(-1, True, needs_parity=True):
                            - b
                          - extra_pulses(-1, True):
                        - retract(-1):
                - less_obs():
                  - ssto ssto
                - pull(2):
                  - more_pistons(1):
                    - e
                  - full_extend(2):
                    - extend(2, False, needs_parity=True):
                      - b
                      - more_obs(True):
                        - bobs bobs
                      - extend(-1, needs_parity=True):
                        - b
                    - extra_pulses(2, False):
                      - b
                  - retract(-1):
                  - less_obs():
                    - ssto ssto
                  - full_row(0):
                    - more_pistons(0):
                      - d
                    - row_high(0):
                      - full_extend(0, False):
                        - extend(0, False, needs_parity=True):
                          - a
                        - extra_pulses(0, False):
                      - retract(0):
                        - pull(-2):
                        - shift_pistons(0):
                          - d c c
                        - row_high(-1, True):
                          - full_extend(-1, True):
                            - extend(-1, True, needs_parity=True):
                              - b
                            - extra_pulses(-1, True):
                          - retract(-1):
                  - shift_pistons(1):
                    - e d
                  - shift_pistons(0):
                    - d c c
                - shift_pistons(2):
                  - f o l d
                - row_high(3, True):
                  - full_extend(3, True):
                    - extend(3, True, needs_parity=True):
                      - more_obs(True):
                        - bobs bobs
                      - more_pistons(0):
                        - d
                      - extend(0, needs_parity=True):
                        - a
                    - extra_pulses(3, True):
                      - a
                  - retract(3):
                    - retract(0):
                      - pull(-2):
                      - shift_pistons(0):
                        - d c c
                      - row_high(-1, True):
                        - full_extend(-1, True):
                          - extend(-1, True, needs_parity=True):
                            - b
                          - extra_pulses(-1, True):
                        - retract(-1):
                    - less_obs():
                      - ssto ssto
                    - pull(1):
                      - more_pistons(0):
                        - d
                      - full_extend(1):
                        - extend(1, False, needs_parity=True):
                          - b
                          - a
                        - extra_pulses(1, False):
                      - retract(-2):
                      - full_row(-1):
                        - row_high(-1):
                          - full_extend(-1, False):
                            - extend(-1, False, needs_parity=True):
                              - b
                            - extra_pulses(-1, False):
                          - retract(-1):
                      - shift_pistons(0):
                        - d c c
                    - row_high(2, True):
                      - full_extend(2, True):
                        - extend(2, True, needs_parity=True):
                          - more_obs(True):
                            - bobs bobs
                          - extend(-1, needs_parity=True):
                            - b
                        - extra_pulses(2, True):
                          - b
                      - retract(2):
                        - less_obs():
                          - ssto ssto
                        - pull(0):
                          - more_pistons(0):
                            - d
                          - full_extend(0):
                            - extend(0, False, needs_parity=True):
                              - a
                            - extra_pulses(0, False):
                          - retract(-3):
                          - full_row(-2):
                          - shift_pistons(0):
                            - d c c
                        - shift_pistons(1):
                          - e d
                        - row_high(1, True):
                          - full_extend(1, True):
                            - extend(1, True, needs_parity=True):
                              - a
                            - extra_pulses(1, True):
                          - retract(1):
                            - pull(-1):
                              - b
                            - row_high(0, True):
                              - full_extend(0, True):
                                - extend(0, True, needs_parity=True):
                                  - a
                                - extra_pulses(0, True):
                              - retract(0):
                                - pull(-2):
                                - shift_pistons(0):
                                  - d c c
                                - row_high(-1, True):
                                  - full_extend(-1, True):
                                    - extend(-1, True, needs_parity=True):
                                      - b
                                    - extra_pulses(-1, True):
                                  - retract(-1):